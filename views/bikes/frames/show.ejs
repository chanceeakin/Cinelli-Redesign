<%- include("../../partials/header") %>

                  
    <div id="showCarousel" class="carousel slide" data-interval="false" data-ride="carousel">
      <!-- Indicators -->
      <ul class="carousel-indicators">
        <li data-target="#showCarousel" data-slide-to="0" class="active"></li>
        <li data-target="#showCarousel" data-slide-to="1"></li>
        <li data-target="#showCarousel" data-slide-to="2"></li>
      </ul>
      
      <!-- The slideshow -->
      <div class="carousel-inner">
        <% var gallery = frame.gallery %>                         
        <% gallery.forEach(function(img) { %> 
       <div class="carousel-item">
          <img src="<%= img %>">
        </div>
        <% }); %>
        <div class="carousel-item active">
          <img src="<%= frame.mainImage %>">
        </div>
      </div>
      
      <!-- Left and right controls -->
      <a class="carousel-control-prev carouselIcon" href="#showCarousel" data-slide="prev">
        <span class=""><i class="fas fa-angle-left fa-2x"></i></span>
      </a>
      <a class="carousel-control-next carouselIcon" href="#showCarousel" data-slide="next">
        <span class=""><i class="fas fa-angle-right fa-2x"></i></span>
      </a>
    </div>
    <div id="borderHeaderBottom">
      <ul> 
          <li class="bottomBorderColors" id="red"></li>
          <li class="bottomBorderColors" id="yellow"></li>
          <li class="bottomBorderColors" id="black"></li>
          <li class="bottomBorderColors" id="green"></li>
      </ul>
  </div>

  <div class="caption-full">
    <div id="itemDescription">
      <h4 id="itemName"><a> <%= frame.name %> </a></h4>
      <h4 class="" id="price">$<%= frame.price.toLocaleString(); %>.00</h4>
    </div>
    <div id="itemButtons">
        <button id="addToCart">Add to Cart</button>
        <select name="bikeSize" class="custom-select" id="" aria-label="">
            <% var sizes = frame.sizes %>                         
            <% sizes.forEach(function(bikeSize) { %> 
            <option value="<%= bikeSize.size %>"> <%= bikeSize.size %> </option>
            <% }); %>
        </select>
    </div>
    
      <!-- ADMIN ADD/DELETE BUTTONS -->
        <div id="editDeleteButtons">
          <a id="editButton" class="btn btn-xs btn-warning" href="/shop/bikes/<%= frame.id %>/edit">Edit</a>
          <form id="delete-form" action="/shop/bikes/<%= frame._id %>?_method=DELETE" method="POST">
              <button id="deleteButton" class="btn btn-xs btn-danger">Delete</button>
          </form>
        </div>
  </div>

  <!-- DESCRIPTION -->
  <div id="itemParagraph">
    <p> <%= frame.description %> </p>
  </div>
  <div id="colorSeparator"></div>

  <!-- SPECIFICATIONS -->
  <div class="container-fluid" id="technicalSpecifications">
    <div class="row justify-content-center">
      <div class="col-lg-12">
        <h2>Technical Specifications</h2>
        <!-- FRAMESET -->
          <div class="specificationCategories">
            <h5>Frameset</h5>
              <div>
                <h6>Frame</h6>
                <p><%= frame.frame %></p>
              </div>
                <div>
                  <h6>Fork</h6>
                  <p><%= frame.fork %></p>
                </div>
              <div>
                <h6>Weight</h6>
                <p><%= frame.weight %></p>
              </div>
            </div>
            <!-- COCKPIT -->
            <div class="specificationCategories">
              <h5>Cockpit</h5>
                <div>
                  <h6>Seatpost</h6>
                  <p><%= frame.seatpost %></p>
                </div>
                <div>
                  <h6>Seat Clamp</h6>
                  <p><%= frame.seatClamp %></p>
                </div>
                <div>
                  <h6>Cable Routing</h6>
                  <p><%= frame.cableRouting %></p>
                </div>
                <div>
                  <h6>Headset</h6>
                  <p><%= frame.headset %></p>
                </div>
            </div>
            <!-- BRAKES -->
            <div class="specificationCategories">
              <h5>Brakes</h5>
                <div>
                  <h6>Brakes</h6>
                  <p><%= frame.brakes %></p>
                </div>
              </div>
            <!-- DRIVETRAIN -->
            <div class="specificationCategories">
              <h5>Drivetrain</h5>
                <div>
                  <h6>BottomBracket</h6>
                  <p><%= frame.bottomBracket %></p>
                </div>
              </div>
            <!-- WHEELS -->
            <div class="specificationCategories">
              <h5>Wheels</h5>
                <div>
                  <h6>Tire Clearance</h6>
                  <p><%= frame.tireClearance %></p>
                </div>
                <div>
                  <h6>Hub Spacing</h6>
                  <p><%= frame.hubSpacing %></p>
                </div>
              </div>

      </div>
    </div>
  </div>


<%- include("../../partials/footer") %>